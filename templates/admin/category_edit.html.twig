{% extends 'base.html.twig' %}

{% block title %}Editar Categoría{% endblock %}

{% block body %}
    <h1 class="mb-4">Editar Categoría: {{ category.name }}</h1>

    <div class="card">
        <div class="card-body">
            <form method="post">
                <div class="mb-3">
                    <label for="name" class="form-label">Nombre *</label>
                    <input type="text"
                           class="form-control"
                           id="name"
                           name="name"
                           value="{{ category.name }}"
                           required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Tipo</label>
                    <input type="text"
                           class="form-control"
                           value="{{ category.type }}"
                           disabled>
                    <small class="text-muted">El tipo no se puede cambiar después de crear la categoría</small>
                </div>

                <div class="mb-3">
                    <label for="description" class="form-label">Descripción</label>
                    <textarea class="form-control"
                              id="description"
                              name="description"
                              rows="3">{{ category.description }}</textarea>
                </div>

                <hr>

                <h5>Elementos de la categoría</h5>

                <div class="table-responsive">
                    <table class="table table-sm">
                        <thead>
                        <tr>
                            <th width="50">
                                <input type="checkbox" id="selectAll" onclick="toggleAll(this)">
                            </th>
                            <th width="80">Foto</th>
                            <th>Nombre</th>
                            <th>Equipo</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for item in availableItems %}
                            <tr>
                                <td>
                                    <input type="checkbox"
                                           name="items[]"
                                           value="{{ item.id }}"
                                           class="item-checkbox"
                                           {% if item.id in currentItems %}checked{% endif %}>
                                </td>
                                <td>
                                    {% if item.photo %}
                                        <img src="{{ item.photo }}" width="40" class="rounded-circle">
                                    {% endif %}
                                </td>
                                <td><strong>{{ item.name }}</strong></td>
                                <td>{{ item.team }}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>

                <div class="mt-3">
                    <button type="submit" class="btn btn-primary">💾 Guardar Cambios</button>
                    <a href="{{ path('app_admin') }}" class="btn btn-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        function toggleAll(source) {
            const checkboxes = document.querySelectorAll('.item-checkbox');
            checkboxes.forEach(checkbox => {
                checkbox.checked = source.checked;
            });
        }
    </script>
{% endblock %}
