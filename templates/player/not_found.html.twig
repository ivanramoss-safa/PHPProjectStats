{% extends 'base.html.twig' %}

{% block title %}Jugador no disponible{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 text-center">
            <div class="card border-0 shadow-sm p-5">
                <div class="mb-4" style="font-size: 4rem;">⚽</div>
                <h2 class="fw-bold mb-3">Datos no disponibles</h2>
                <p class="text-muted mb-4">
                    No hemos podido encontrar los datos de este jugador. 
                    Es posible que el jugador no esté registrado en la base de datos 
                    o que los datos no estén disponibles en este momento.
                </p>
                <div class="d-flex justify-content-center gap-3">
                    <a href="{{ path('player_index') }}" class="btn btn-primary">
                        🔍 Buscar jugadores
                    </a>
                    <a href="javascript:history.back()" class="btn btn-outline-secondary">
                        ← Volver atrás
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
